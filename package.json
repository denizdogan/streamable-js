{
  "name": "streamable-js",
  "version": "0.0.1",
  "description": "A Node.js API for Streamable with Promises",
  "main": "src/index.js",
  "scripts": {
    "compile": "babel -d src lib",
    "watch": "babel -w -d src lib",
    "uglify": "babel --minified --compact true --no-comments -d src lib",
    "dist": "npm run uglify"
  },
  "author": "Deniz Dogan <deniz@dogan.se>",
  "license": "MIT",
  "dependencies": {
    "request-promise": "^2.0.0",
    "url-join": "0.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0"
  }
}
